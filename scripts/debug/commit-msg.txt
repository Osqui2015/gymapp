feat(stats+drilldown): Fase 1.3 + drilldown de músculo

Fase 1.3 — Stats agregadas estilo openGym (streak + heatmap):

Endpoints nuevos:
- GET /api/stats/resumen — current_streak, longest_streak, this_week,
  this_month, last_30_days, total_workouts, total_sets (cache 5min)
- GET /api/stats/heatmap?weeks=53 — por fecha: sets + volumen total
  (cache 10min)

Componentes nuevos:
- StreakCard.vue: card con icono de llama + racha grande + 3 sub-stats
  (esta semana, este mes, mejor racha) + totales
- ActivityHeatmap.vue: grid 53×7 estilo GitHub, color por nivel de
  sets en el día, hover con detalle

Integración en HistorialContent.vue (siempre visibles en el tab
"Matriz de Progreso", arriba del matrix):
- grid md:grid-cols-3 con StreakCard (1 col) + Heatmap (2 col)
- Carga en onMounted y en onAlumnoChange (cuando cambia el alumno)

Drilldown de músculo (mejora sobre el body map):
- GET /api/body-map/muscle/{slug}/exercises — lista de ejercicios
  que trabajan ese músculo, ordenados por volumen reciente (30d)
- Modal en HistorialContent.vue al clickear un músculo en el body map:
  muestra los ejercicios + sets_30d + max_peso_30d
- Cerrar con click afuera o botón X

Verificado:
- npm run build (ok)
- php artisan test (171/171)
